# EpiLink Environment

# FastAPI backend base URL — REQUIRED for live data.
VITE_API_BASE_URL=http://localhost:8000

# OPTIONAL: Mapbox token. If unset, the map falls back to OpenStreetMap.
# VITE_MAPBOX_TOKEN=
# VITE_MAPBOX_STYLE=mapbox/light-v11
